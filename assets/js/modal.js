const openModal = (id) => {
    const modal = document.getElementById(id);
    modal.style.display = "flex";
}

const closeModal = (id) => {
    const modal = document.getElementById(id);
    modal.style.display = "none";
}
